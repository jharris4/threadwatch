config/config.toml
config/devices.json
config/devices.json.lock
data/
.venv-flash/
.nrfutil-bin/
__pycache__/
*.pyc
config/credentials.toml
config/alerts.env
config/ha.env
.DS_Store
# What a deploy without a .git records as the revision it copied
# (bin/push-to-host.sh writes it on the host, never here).
REVISION

.venv/
