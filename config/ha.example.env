# Home Assistant access for `threadwatch import-ha` (copy to config/ha.env,
# chmod 600; gitignored). docs/HOME-ASSISTANT.md walks through the token.
HA_URL=http://homeassistant.local:8123
HA_TOKEN=paste-a-long-lived-access-token-here
